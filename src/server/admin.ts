import { Command } from "./chat";

new Command("spawnveh" ,(player: PlayerMp, args: any) => {
    console.log(args);
});new Command("spawncar" ,(player: PlayerMp, args: any) => {
    console.log(args);
});